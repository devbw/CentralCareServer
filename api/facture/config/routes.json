{
  "routes": [
    {
      "method": "GET",
      "path": "/factures",
      "handler": "facture.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/factures/count",
      "handler": "facture.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/factures/:id",
      "handler": "facture.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/factures",
      "handler": "facture.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/factures/:id",
      "handler": "facture.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/factures/:id",
      "handler": "facture.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
